#include <stdio.h>

int main() {
    int arr[5] = {10, 20, 30, 40, 50};

    // Updating elements
    arr[2] = 35;

    // Print the updated array
    printf("Updated array: ");
    for (int i = 0; i < 5; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");

    return 0;
}
